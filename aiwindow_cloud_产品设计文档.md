---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 06c55cafedf6bf51e8bda6d121982815_c8dedf74a6b911f1891f525400f8a581
    ReservedCode1: Y4n08VEY+Rn6DtjGgKcHk7hK5DYsEMhZiX/JLSZW5DFdt/IQNl0OYb/e5AhXMFFBZFFtSemRONmwuB8Y2oyt5ZM6BMYHVksXrd9vVhvvFN/Yq/Eb2Fc/LPjEsSz4odfXP83CXhXbAhcUUVwaWIvm8gr7d1ml297PLH4J5nUlL6HCmngCe/Fj6h8dBp0=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 06c55cafedf6bf51e8bda6d121982815_c8dedf74a6b911f1891f525400f8a581
    ReservedCode2: Y4n08VEY+Rn6DtjGgKcHk7hK5DYsEMhZiX/JLSZW5DFdt/IQNl0OYb/e5AhXMFFBZFFtSemRONmwuB8Y2oyt5ZM6BMYHVksXrd9vVhvvFN/Yq/Eb2Fc/LPjEsSz4odfXP83CXhXbAhcUUVwaWIvm8gr7d1ml297PLH4J5nUlL6HCmngCe/Fj6h8dBp0=
---

# aiwindow_cloud 智能云端助手 产品设计文档

- 文档版本：v1.0
- 软件版本：aiwindow_cloud v1.6.8
- 文档日期：2026-09-02

---

## 一、产品概述

aiwindow_cloud 是一款面向国产化桌面操作系统（龙芯 LoongArch64 + 银河麒麟、统信 UOS、openEuler 等）的轻量化「划词式」智能云端助手。产品以一枚可拖拽、可吸附屏幕边缘的悬浮球为入口，用户选中任意文字即可一键唤起 AI，完成问答、代码解释、文本润色等任务，全程免切换窗口、免敲命令。

产品强调「轻、快、安全、零运维」：

- **轻**：纯 Python + GTK3 实现，无沉重框架，常驻内存目标 ≤300MB；
- **快**：单窗口双形态，点一下球即展开对话，操作路径最短；
- **安全**：纯 GUI 单进程架构，不监听任何端口、不对外暴露 HTTP 服务，配置经 AES-256 加密落库；
- **零运维**：安装即用，开机自动常驻，无需任何手动后台配置。

---

## 二、设计思路

### 2.1 设计理念

1. **对话即划词**：将 AI 能力从「专门的聊天窗口」中解放出来，嵌入用户的真实工作流——选中文字 → 浮出能力 → 得到结果，符合「最短操作路径」原则。
2. **一个窗口，两种形态**：收缩态仅一枚悬浮球（56×56px），不占用工作区；展开态为「悬浮球 + 聊天面板」同窗结构，可整体同步拖拽，兼顾轻打扰与功能完整。
3. **桌上桌面亲缘设计**：优先服务国产化 Linux 桌面用户，从渲染（Cairo）、打包（rpm/deb）、自启（systemd + XDG autostart）全链路原生适配，而非简单套用 Web 方案。

### 2.2 交互设计思路

- **无设置负担的首次启动**：随包分发 AES 加密的种子密钥（`seed_key.enc` + `bootstrap.key` 配对），用户解压后首次运行自动导入，开箱即用；
- **三种高频诉求一键切换**：问答 / 代码解释 / 文本润色，通过下拉模式选择即时切换，每种模式内置独立的系统提示词；
- **符合桌面习惯的输入**：Enter 发送、Shift+Enter 换行；窗口拖拽后自动吸附屏幕边缘（SNAP_MARGIN=40px 容差）；
- **「退出」多入口兜底**：托盘菜单、设置页「退出应用」按钮（带确认框）双出口，杜绝「关窗即退」等误操作导致的数据丢失。

### 2.3 技术选型思路

| 决策点 | 选型 | 理由 |
|---|---|---|
| 交互框架 | GTK3 + PyGObject | 国产桌面（银河麒麟/麒麟服务器版）默认集成，免额外重依赖 |
| 架构模式 | 纯 GUI 单进程（无 HTTP） | 消除端口监听与越权攻击面，符合信息安全要求 |
| 配置存储 | SQLite + AES-256-CBC 加密 | 结构化、可移植；密钥等敏感项不落明文 |
| 云端接入 | OpenAI 兼容 API（urllib 直调） | 标准协议，可平滑切换智谱 / OpenAI / DeepSeek 等平台 |
| 打包交付 | rpm / deb 双格式 + 内置纯 Python 兜底打包器 | 覆盖 openEuler / 麒麟 / Debian / Ubuntu 全系 |

---

## 三、需求分析

### 3.1 目标用户

- 国产化办公场景下的知识工作者：需要快速检索、润色文档、理解代码；
- 龙芯、鲲鹏等信创硬件上的政企办公人员：对安全合规、低资源占用有硬性要求；
- 开发者：需要随时划词解释代码片段。

### 3.2 功能需求（FR）

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-1 | 划词唤起 | 复制文字后自动带入输入框，无需手动粘贴 |
| FR-2 | 三模式对话 | 问答 / 代码解释 / 文本润色，独立系统提示词 |
| FR-3 | 双形态悬浮窗 | 收缩态悬浮球 / 展开态球+面板同窗，可拖拽、吸附边缘 |
| FR-4 | 设置管理 | 在线配置 API Key / base_url / model / timeout |
| FR-5 | 模型连接状态监测 | 打开设置页自动探测云端连通性与鉴权状态 |
| FR-6 | 后台驻留 | 关闭窗口仅隐藏、后台常驻，多出口彻底退出 |
| FR-7 | 开机自启动 | systemd 服务 + 桌面 autostart 双通道 |
| FR-8 | 调用日志 | 记录云端调用状态与摘要，可查看、可清空 |

### 3.3 非功能需求（NFR）

| 编号 | 需求 | 指标 |
|---|---|---|
| NFR-1 | 性能 | 常驻内存目标 ≤300MB（systemd MemoryMax=150M） |
| NFR-2 | 安全 | 敏感项 AES-256 加密存储；0 个监听端口 |
| NFR-3 | 兼容性 | LoongArch64 / arm64 / amd64；五类主流 Linux 发行版 |
| NFR-4 | 可靠性 | 网络异常、鉴权失败均有友好提示与自动降级 |
| NFR-5 | 可维护性 | 配置路径稳定，支持旧库自动迁移；模块化可单测 |

### 3.4 需求优先级

P0（核心）：FR-1/2/4/5、NFR-1/2、NFR-3。
P1（体验）：FR-3/6/7/8。
P2（扩展）：多平台模型模板、thinking 深度思考透传。

---

## 四、系统架构与解决方案

### 4.1 总体架构

采用「三模块单进程」架构，无网络服务层，全部能力在进程内完成：

```
┌──────────────────────────── aiwindow_cloud（单进程） ───────────────────────────┐
│  floating_window.py（GUI 表现层）                                                │
│   悬浮球 / 聊天面板 / 三模式 / 划词带入 / 设置页 / 托盘 / 自启渲染                │
│        │ 进程内直调                                                            │
│  backend.py（业务核心层）                                                        │
│   配置管理(SQLite) / AES密钥 / 种子导入 / 云端转发 / 探测 / 日志 / 迁移           │
│        │ 标准库 urllib（加密通道）                                              │
│  crypto.py（安全工具层）  AES-256-CBC 加解密 / 密码生成                          │
└──────────────────────────────────────────────────────────────────────────────────┘
         │
    HTTPS（无监听端口，仅出向请求）
  智谱 / OpenAI / DeepSeek / 自定义兼容端点
```

### 4.2 模块设计

**floating_window.py（表现层）**
- 窗口生命周期：单 `Gtk.Window` 承载收缩/展开双形态，动画过渡（16ms 帧）；
- 消息组装：`build_messages()` 依据当前模式注入系统提示词，携带最近 8 条历史；
- 请求发起：`call_backend()` 进程内直调 `backend.handle_chat()`，取消 HTTP 中转，降低延迟与风险。

**backend.py（核心层）**
- **配置持久化**：数据目录锁定 `~/.config/aiwindow`（环境变量显式设置时优先且不降级）；首次启动自动将旧库（`~/.local/share`、`/var/lib`）一次性迁移，迁移逻辑「新库已有配置绝不覆盖」；
- **密钥安全**：API Key 经 AES-256-CBC 加密后入 SQLite；存取自动 strip 首尾空白，根治 403/401 类鉴权偶发问题；
- **种子自动导入**：随包 `seed_key.enc` + `bootstrap.key` 配对文件，首次运行 `ensure_default_key_from_seed()` 自动解密导入，默认预置智谱 base_url 与模型；
- **多平台模型通用化**：内置智谱 / OpenAI / DeepSeek / 自定义四套模板；`thinking`（深度思考）私有字段仅在智谱平台下发，其余平台剔除避免 400；
- **智能探测与降级**：云端连通探测定时缓存（TTL 机制）；`/models` 返回 404/405 时自动降级 `/chat/completions` 探测；401 鉴权失败不降级、如实上报；
- **友好错误语义**：从响应体 `error.message` 提取平台原始错误，403 明确提示「无权访问模型」而非笼统「密钥无效」。

**crypto.py（安全层）**
- 基于系统 openssl 实现 AES-256-CBC 加解密，输出无换行单行 base64；
- 解密端兼容 strip 尾随换行与 CRLF/LF 差异，规避跨平台「error reading input file」历史问题。

### 4.3 安全方案

| 威胁面 | 应对 |
|---|---|
| 配置明文泄露 | API Key 等敏感项 AES-256 加密后再落 SQLite，磁盘不落明文 |
| 网络攻击面 | 纯 GUI 单进程、无端口监听、无 HTTP 服务，从架构上消除远程入侵入口 |
| 密钥分发安全 | 种子密钥随包 AES 加密，配对 `bootstrap.key` 解密，防静态泄露 |
| 配置漂移 | 数据目录锁定单一路径 + 旧库一次性迁移 + 不覆盖已有配置 |

### 4.4 部署方案

- **一键构建**：`build.sh` 自动检测发行版，默认产出 rpm，`AIWINDOW_DEB=1` 产出 deb；无 `dpkg-deb` 时自动调用内置纯 Python 兜底打包器；
- **双通道自启**：`aiwindow-backend.service`（`WantedBy=multi-user.target`）负责开机初始化数据，`/etc/xdg/autostart/aiwindow-floating.desktop` 负责桌面登录后拉起悬浮球 GUI；
- **版本管理**：构建时 `AIWINDOW_API_KEY` 可选预置默认密钥，其余场景安装后于设置页填写。

---

## 五、创新点

1. **「划词即服务」的轻交互范式**：将 AI 对话能力封装为悬浮球形态，嵌入用户原生工作流，非侵入、即取即用；
2. **纯 GUI 单进程安全架构**：在同类 AI 助手中率先取消本地 HTTP 服务与端口监听，从根本上压缩攻击面，契合信创环境高安全要求；
3. **零明文密钥落地 + 种子自动分发**：AES-256 加密存储 + 随包种子自动导入，兼具安全与「装完即用」的体验；
4. **多平台模型协议解耦**：一套代码同时兼容智谱 / OpenAI / DeepSeek 等平台；私有字段（thinking）按平台智能下发，避免 400/404；
5. **自愈式探测降级**：`/models` 404/405 → 自动降级探测、结果 TTL 缓存，弱网与异构网关下仍可判定连通状态；
6. **全信创链路交付**：LoongArch64 交叉适配、rpm/deb 双格式、systemd + autostart 双通道自启，覆盖国产桌面主流部署形态。

---

## 六、性能与可靠性指标

| 指标 | 目标值 | 验证方式 |
|---|---|---|
| 常驻内存 | ≤300MB（systemd Limit 150M） | systemd 状态监控 / 实测 |
| 悬浮球交互延迟 | 点按即时响应（无感知） | 交互实测 |
| 云端首次连接探测 | 结果缓存，重复打开免等待 | 自动化用例 |
| 配置持久化 | 重启跨会话保持同一库 | 回归测试 attest_persist |
| 旧数据迁移 | 一次性自动迁移、绝不覆盖 | 自动化用例 |

---

## 七、总结

aiwindow_cloud 以「划词即用、轻量安全、信创原生」为设计主线，通过双形态悬浮窗交互、加密配置管理、多平台模型协议解耦与自愈式探测等技术方案，在国产化桌面环境下提供了安全、低占用、高可用的智能助手体验。产品具备清晰的模块边界与完整的自动化测试支撑，可独立交付、独立演进，符合揭榜挂帅对创新性、实用性与安全合规的综合要求。

